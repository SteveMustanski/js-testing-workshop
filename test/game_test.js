const expect = require('chai').expect;

describe('GAME INSTANCE FUNCTIONS', function (){
  describe('checkGameSatus', function () {
    // pending tests do not have callback
    // or would have an x infront like 'xdescribe' or 'xit'
    it('should tell me when the game is over');
  })
})